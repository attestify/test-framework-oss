# Attestify Test Framework - Open Source

[package]
name = "attestify_test_framework"
version = "0.2.0"
edition = "2024"

[features]
# keep assertions available by default, nape_filesystem opt-in
default = ["assertions"]
assertions = []
nape_filesystem = []
